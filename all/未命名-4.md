```mermaid
flowchart LR
    Player[[Player]]
    Player -->|上传视频| UploadService["Upload Service"]
    UploadService -->|投递任务| TaskQueue["Task Queue"]
    TaskQueue -->|姿态估计 & SMPL 重建| GPUWorker["GPU Worker"]
    GPUWorker -->|BVH 输出| DCCWorker["DCC Worker"]
    DCCWorker -->|保存动画 Asset| AssetStore["Asset Store"]
    AssetStore --> CDN["CDN/边缘缓存"]
    CDN -->|拉取并播放动画| Player
```